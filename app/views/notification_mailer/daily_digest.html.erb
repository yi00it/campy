<!DOCTYPE html>
<html>
<head>
  <meta content='text/html; charset=UTF-8' http-equiv='Content-Type' />
  <style>
    body { font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Arial, sans-serif; line-height: 1.6; color: #333; margin: 0; padding: 0; background: #f6f7f9; }
    .container { max-width: 600px; margin: 0 auto; padding: 20px; }
    .header { background: #4f7ae1; color: white; padding: 30px 20px; text-align: center; border-radius: 8px 8px 0 0; }
    .content { background: #ffffff; padding: 30px; border: 1px solid #e0e4ea; border-top: none; }
    .section { margin: 30px 0; }
    .section-title { color: #4f7ae1; font-size: 18px; font-weight: 600; margin-bottom: 15px; padding-bottom: 10px; border-bottom: 2px solid #e0e4ea; }
    .notification-item { background: #f6f7f9; padding: 15px; margin: 10px 0; border-radius: 6px; border-left: 3px solid #4f7ae1; }
    .button { display: inline-block; background: #4f7ae1; color: white; padding: 12px 24px; text-decoration: none; border-radius: 6px; margin: 20px 0; }
    .footer { text-align: center; padding: 20px; color: #8a94a3; font-size: 14px; }
  </style>
</head>
<body>
  <div class="container">
    <div class="header">
      <h1 style="margin: 0;">Your Daily Digest</h1>
      <p style="margin: 10px 0 0 0;">
        <%= @unread_count %> new <%= 'notification'.pluralize(@unread_count) %>
      </p>
    </div>

    <div class="content">
      <h2>Hi <%= @user.display_name %>,</h2>

      <% [@activity_notifications, @comment_notifications, @message_notifications, @project_notifications].each do |notifications| %>
        <% notifications.each do |notification| %>
          <div class="notification-item">
            <p><%= notification.message %></p>
          </div>
        <% end %>
      <% end %>

      <center>
        <a href="<%= notifications_url %>" class="button">View All</a>
      </center>
    </div>

    <div class="footer">
      <p>© <%= Time.current.year %> Campy</p>
    </div>
  </div>
</body>
</html>

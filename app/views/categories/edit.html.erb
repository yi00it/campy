<section class="section">
  <header class="section__head">
    <h1 class="section__title">Rename category</h1>
    <p class="section__lead">Give this category a clearer name. Short, confident labels keep Basecamp-style focus.</p>
  </header>

  <div class="card">
    <%= render "form", category: @category %>
  </div>

  <div class="hero__cta mt-md">
    <%= link_to "Back to categories", categories_path, class: "btn btn--text" %>
  </div>
</section>

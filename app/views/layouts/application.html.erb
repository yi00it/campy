<!DOCTYPE html>
<html>
  <head>
    <title>Campy</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>
  <body class="app" <%= 'data-theme="dark"' if current_theme == "dark" %>>
    <header class="site-header">
      <div class="site-header__inner">
        <%= link_to (user_signed_in? ? authenticated_root_path : root_path), class: "brand" do %>
          <span class="brand__badge">C</span>
          <span>Campy</span>
        <% end %>
        <nav class="nav">
          <% if user_signed_in? %>
            <%= link_to "Projects", projects_path, class: "nav-link" %>
            <%= link_to "My activities", my_activities_path, class: "nav-link" %>
            <%= link_to "Zones", zones_path, class: "nav-link" %>
            <%= link_to "Disciplines", disciplines_path, class: "nav-link" %>
            <%= link_to "Messages", conversations_path, class: "nav-link" %>
            <%= link_to "Settings", settings_path, class: "btn btn--ghost" %>
            <%= link_to "Sign out", destroy_user_session_path, data: { turbo_method: :delete }, class: "btn btn--ghost" %>
          <% else %>
            <%= link_to "Sign in", new_user_session_path, class: "nav-link" %>
            <%= link_to "Start your free trial", new_user_registration_path, class: "btn btn--primary" %>
          <% end %>
        </nav>
      </div>
    </header>

    <main class="page main-content">
      <%= render "shared/flash" %>
      <%= yield %>
    </main>

    <footer class="site-footer">
      <small>Campy helps teams stay on track. © <%= Time.current.year %></small>
    </footer>
  </body>
</html>

<section class="section">
  <header class="section__head">
    <h1 class="section__title">Add calendar event</h1>
    <p class="section__lead">Create personal tasks or meetings that appear alongside your assigned activities.</p>
  </header>

  <div class="card">
    <%= render "form", calendar_event: @calendar_event %>
  </div>
</section>

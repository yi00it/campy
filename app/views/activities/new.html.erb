<section class="section">
  <header class="section__head">
    <h1 class="section__title">Plan a new activity</h1>
    <p class="section__lead">Keep it short, sharp, and reassuring—just like Basecamp. Set the zone and discipline so the team knows where the work sits.</p>
  </header>

  <div class="card card--soft mb-md">
    <div class="list-inline list-inline--spread">
      <p class="meta">
        Importing a whole schedule? Upload an Excel template to create activities in bulk instead.
      </p>
      <%= link_to "Import from Excel", new_project_activities_import_path(@project), class: "btn btn--secondary" %>
    </div>
  </div>

  <div class="card">
    <%= render "form", activity: @activity, project: @project, disciplines: @disciplines, zones: @zones %>
  </div>

  <div class="hero__cta mt-md">
    <%= link_to "Back to project", project_path(@project), class: "btn btn--text" %>
  </div>
</section>

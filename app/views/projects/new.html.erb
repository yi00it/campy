<section class="section">
  <header class="section__head">
    <h1 class="section__title">Create a new project</h1>
    <p class="section__lead">Kick off something fresh. Keep the voice friendly, inviting, and Basecamp-inspired.</p>
  </header>

  <div class="card">
    <%= render "form", project: @project %>
  </div>

  <div class="hero__cta mt-md">
    <%= link_to "Back to projects", projects_path, class: "btn btn--text" %>
  </div>
</section>

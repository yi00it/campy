<div class="card sticky-card" id="project-heartbeat">
  <h3>Project heartbeat</h3>
  <p class="meta">Campy captures the Basecamp vibe by keeping things human. Share progress updates, celebrate wins, and course correct without panic.</p>
  <hr class="hr">
  <ul class="list-reset stack">
    <li><strong><%= active_count + completed_count %></strong> activities currently tracked.</li>
    <li><strong><%= completed_count %></strong> marked done.</li>
    <li><strong><%= active_count %></strong> still in flight.</li>
  </ul>
</div>
